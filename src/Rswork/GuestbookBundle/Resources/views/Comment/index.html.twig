<h1>Comment list</h1>

<table class="records_list">
    <thead>
        <tr>
            <th>Id</th>
            <th>Author</th>
            <th>Content</th>
            <th>Message_id</th>
            <th>Published</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td><a href="{{ path('guestbook_comment_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
            <td>{{ entity.author }}</td>
            <td>{{ entity.content }}</td>
            <td>{{ entity.messageid }}</td>
            <td>{% if entity.published %}{{ entity.published|date('Y-m-d H:i:s') }}{% endif %}</td>
            <td>
                <ul>
                    <li>
                        <a href="{{ path('guestbook_comment_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('guestbook_comment_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>

<ul>
    <li>
        <a href="{{ path('guestbook_comment_new') }}">
            Create a new entry
        </a>
    </li>
</ul>
