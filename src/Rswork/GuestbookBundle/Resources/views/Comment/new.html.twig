<h1>Comment creation</h1>

<form action="{{ path('guestbook_comment_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('guestbook_comment') }}">
            Back to the list
        </a>
    </li>
</ul>
